{
  "name": "@yilink/web",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "next dev --webpack",
    "build": "next build --webpack",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "e2e": "playwright test",
    "db:generate": "XDG_CACHE_HOME=${XDG_CACHE_HOME:-../../.cache} DATABASE_URL=${DATABASE_URL:-file:./data/yilink.db} prisma generate",
    "db:migrate": "XDG_CACHE_HOME=${XDG_CACHE_HOME:-../../.cache} DATABASE_URL=${DATABASE_URL:-file:./data/yilink.db} prisma migrate dev",
    "db:deploy": "XDG_CACHE_HOME=${XDG_CACHE_HOME:-../../.cache} DATABASE_URL=${DATABASE_URL:-file:./data/yilink.db} prisma migrate deploy",
    "db:seed": "XDG_CACHE_HOME=${XDG_CACHE_HOME:-../../.cache} DATABASE_URL=${DATABASE_URL:-file:./data/yilink.db} node prisma/seed.mjs",
    "admin:grant": "node scripts/grant-admin.mjs",
    "stats:rollup": "DATABASE_URL=${DATABASE_URL:-file:./data/yilink.db} node scripts/rollup-daily.mjs",
    "test": "vitest run",
    "cf:build": "opennextjs-cloudflare build",
    "cf:preview": "opennextjs-cloudflare build && wrangler dev",
    "cf:deploy": "opennextjs-cloudflare build && wrangler deploy",
    "cf:d1:schema": "XDG_CACHE_HOME=${XDG_CACHE_HOME:-../../.cache} prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > cloudflare/d1-schema.sql",
    "cf:d1:apply:local": "wrangler d1 execute yilink-db --local --file cloudflare/d1-schema.sql",
    "cf:d1:apply:remote": "wrangler d1 execute yilink-db --remote --file cloudflare/d1-schema.sql",
    "cf:d1:tag:apply:local": "wrangler d1 execute yilink-tag-cache --local --file cloudflare/tag-cache-schema.sql",
    "cf:d1:tag:apply:remote": "wrangler d1 execute yilink-tag-cache --remote --file cloudflare/tag-cache-schema.sql"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "@opennextjs/cloudflare": "^1.20.2",
    "@prisma/adapter-d1": "^6.19.2",
    "@prisma/client": "6.19.2",
    "@yilink/icons": "workspace:*",
    "@yilink/moderation": "workspace:*",
    "@yilink/shared": "workspace:*",
    "bcryptjs": "^3.0.2",
    "next": "16.2.12",
    "next-auth": "5.0.0-beta.32",
    "next-intl": "4.13.4",
    "qrcode": "^1.5.4",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.3.3",
    "@types/qrcode": "^1.5.6",
    "prisma": "6.19.2",
    "tailwindcss": "4.3.3",
    "wrangler": "^4.118.0"
  }
}
